# 动态化与 App 化实施路线图

## 1. 总目标

将当前青海生态旅游静态 Web 原型，逐步升级为一个可真实使用、数据会变化、可管理、可部署，并最终可打包成 App 的文旅服务应用。

当前项目特点：

- 纯静态页面。
- 数据写在 `script.js` 中。
- 搜索、收藏、下单、发帖等交互只在当前页面内临时生效。
- 刷新页面后新增数据不会保存。
- 暂无后端、数据库、账号系统和管理后台。

目标形态：

- 前端界面可持续变化。
- 订单、收藏、发帖、反馈等数据可保存。
- 景点、酒店、康养中心、乡土产品可维护。
- 支持游客端和后续管理端。
- 支持移动端体验，并可进一步打包为 App。

## 2. 实施原则

- 不一次性大重构，采用小步提交。
- 每一步都保持项目可运行。
- 先做数据可保存，再做架构升级。
- 先服务 Web，再扩展 App。
- 所有阶段都避免提交依赖、缓存、日志、密钥和本机运行环境目录。

## 3. 阶段一：让当前前端真正“会变化”

当前进度：已实现订单、收藏、旅趣营帖子和意见反馈的 `localStorage` 本地持久化；已增加“我的”动态统计面板、反馈记录列表、订单待支付/已支付状态、订单状态筛选、支付后状态更新、单条反馈删除、反馈清空和本地数据重置入口。下一步可继续补充更细的表单校验、订单状态流转历史和更完整的反馈管理。

### 目标

在不引入后端的前提下，先让页面具备基础持久化能力。

### 修改方向

- 使用 `localStorage` 保存订单、收藏、发帖和意见反馈。
- 页面加载时从 `localStorage` 恢复用户数据。
- 保留静态景点、酒店、康养中心和产品数据。
- 增加数据为空时的友好提示。
- 增加操作成功、失败、删除确认等基础反馈。

### 预期改动文件

```text
script.js
README.md
```

### 验收标准

- 收藏景点或产品后刷新页面，收藏仍存在。
- 新增订单后刷新页面，订单仍存在。
- 发布旅趣营帖子后刷新页面，帖子仍存在。
- 删除订单或取消收藏后刷新页面，状态保持正确。

## 4. 阶段二：拆分前端数据与逻辑

### 目标

降低 `script.js` 的体积和维护难度，为后续框架化或后端接口化做准备。

### 修改方向

- 将景点、酒店、康养中心、商品、美食等静态数据拆分为独立文件。
- 将本地存储工具函数独立出来。
- 将渲染逻辑、订单逻辑、收藏逻辑、支付演示逻辑逐步分组。
- 保持无构建流程，仍可用静态服务器运行。

### 预期改动文件

```text
script.js
data.js
storage.js
README.md
```

### 验收标准

- 页面功能与拆分前一致。
- `node --check` 检查通过。
- 静态服务器访问首页正常。
- 数据文件和业务逻辑边界更清晰。

## 5. 阶段三：引入前端框架

### 目标

将项目从原生 DOM 操作升级为更适合维护和扩展的现代前端应用。

### 推荐技术

```text
Vue 3 + Vite
```

推荐 Vue 的原因：

- 上手成本较低。
- 模板语法适合当前页面结构迁移。
- 适合表单、列表、弹窗和状态驱动界面。
- 后续可接入 Pinia、Vue Router 和移动端组件库。

### 修改方向

- 创建 Vue 入口。
- 将首页、康养中心、宝藏铺、酒店预订、旅趣营、我的页面拆成组件。
- 用响应式状态替代手动 DOM 拼接。
- 保留当前视觉风格和静态资源。

### 预期改动文件

```text
package.json
src/
index.html
README.md
```

### 验收标准

- `npm install` 后可运行。
- `npm run dev` 正常启动。
- `npm run build` 成功。
- 原有主要功能迁移完成。

## 6. 阶段四：接入后端和数据库

### 目标

让项目拥有真实数据接口和数据持久化能力。

### 推荐技术

```text
Node.js + Express
SQLite
```

起步使用 SQLite 的原因：

- 部署简单。
- 适合课程设计、原型和小型项目。
- 后续可迁移到 MySQL 或 PostgreSQL。

### 修改方向

- 新增后端 API。
- 建立数据表：用户、景点、酒店、康养中心、产品、订单、收藏、帖子、意见反馈。
- 前端通过 API 读取和提交数据。
- 保留本地 mock 数据作为开发兜底。

### 预期接口

```text
GET /api/attractions
GET /api/hotels
GET /api/products
GET /api/wellness
GET /api/posts
POST /api/posts
GET /api/orders
POST /api/orders
GET /api/favorites
POST /api/favorites
DELETE /api/favorites/:id
POST /api/feedback
```

### 验收标准

- 刷新页面后数据仍存在。
- 新增订单写入数据库。
- 新增帖子写入数据库。
- 收藏状态由接口返回。

## 7. 阶段五：用户系统与管理后台

### 目标

让平台具备游客账号体系和乡村经营主体维护能力。

### 修改方向

- 游客注册、登录、退出。
- 用户个人资料。
- 管理员登录。
- 景点、民宿、康养中心、产品的后台增删改查。
- 订单和反馈管理。
- 区分游客、商家、管理员角色。

### 乡村振兴价值

- 村集体、合作社、民宿经营者可以维护自己的资源。
- 平台运营方可以查看游客反馈和产品热度。
- 本地经营主体可以获得数字化经营入口。

## 8. 阶段六：移动端体验和 PWA

### 目标

先让 Web 应用具备接近 App 的移动体验。

### 修改方向

- 优化手机端导航。
- 增加底部 Tab Bar。
- 优化卡片、弹窗、表单和支付演示页面。
- 增加 PWA manifest。
- 增加离线图标、启动图标和基础缓存策略。

### 验收标准

- 手机浏览器访问体验良好。
- 可添加到手机桌面。
- 首页和核心静态资源可被缓存。

## 9. 阶段七：App 打包

### 推荐路线

```text
Web 应用 -> PWA -> Capacitor -> Android/iOS App
```

### 推荐工具

```text
Capacitor
```

原因：

- 可以复用已有 Web 项目。
- 不需要完全重写为原生 App。
- 适合从 H5 平滑过渡到 Android/iOS。

### 后续可选路线

如果目标包含微信小程序、H5、App 多端统一，可以考虑：

```text
uni-app
```

但这会带来更大迁移成本，建议在 Web 版本稳定后再决定。

## 10. 推荐执行顺序

```text
1. 当前原生前端增加 localStorage 持久化。
2. 拆分 script.js 中的数据和业务逻辑。
3. 优化移动端布局。
4. 引入 Vue 3 + Vite 重构前端。
5. 增加 Node.js + Express + SQLite 后端。
6. 增加用户系统和管理后台。
7. 做 PWA。
8. 用 Capacitor 打包 App。
```

## 11. 下一步建议

下一次开发建议从“阶段一”开始：

```text
让订单、收藏、帖子、意见反馈使用 localStorage 保存，刷新页面后仍然存在。
```

这是投入小、收益明显、风险最低的一步，也能立刻让项目从静态原型向可使用应用迈进。
